import { isDate } from './typeChecks.js';
export default function (value) {
    const t = typeof value;
    return (t === 'string' ||
        t === 'number' ||
        t === 'boolean' ||
        t === 'object' ||
        isDate(value) ||
        t === null);
}
